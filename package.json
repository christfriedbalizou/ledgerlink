{
  "name": "project-scaffolding",
  "version": "1.0.0",
  "description": "Base project for OIDC/OAuth2 application setup.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cross-env DATABASE_URL=file:./ledgerlink.db jest",
    "db:generate": "cross-env DATABASE_URL=file:./ledgerlink.db node scripts/generate-schema.js && prisma generate",
    "db:migrate": "cross-env DATABASE_URL=file:./ledgerlink.db node scripts/generate-schema.js && prisma migrate dev --name init --create-only",
    "db:push": "cross-env DATABASE_URL=file:./ledgerlink.db node scripts/generate-schema.js && prisma db push",
    "db:seed": "node src/db/seed.js"
  },
  "keywords": [
    "express",
    "oauth",
    "oidc",
    "docker"
  ],
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^4.21.2",
    "express-session": "^1.18.2",
    "openid-client": "^6.8.0",
    "passport": "^0.7.0"
  },
  "devDependencies": {
    "@prisma/client": "^6.16.2",
    "cross-env": "^10.0.0",
    "jest": "^30.1.3",
    "prisma": "^6.16.2",
    "supertest": "^7.1.4"
  }
}
